# Desktop app (planned)

The current plan is to ship the same web UI as a desktop app:
- Option A: Tauri (recommended for size/perf; needs Rust toolchain in CI)
- Option B: Electron (bigger, simpler ecosystem)

This folder is a placeholder until the web UI stabilizes.

